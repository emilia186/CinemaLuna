<UserControl x:Class="CinemaLuna.UserControls.MenuControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:CinemaLuna.UserControls"
             mc:Ignorable="d" 
             d:Height="35" d:DesignWidth="800">

    <WrapPanel Orientation="Horizontal">
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <Image Source="{StaticResource logo}" Grid.Column="0" Width="150" Margin="20,30" VerticalAlignment="Top"/>
            <Button Content="Repertuar" Style="{StaticResource NormalButton}" Grid.Column="1"/>
            <Button Content="Cennik" Style="{StaticResource NormalButton}" Grid.Column="2"/>
            <Button Content="Zapowiedzi" Style="{StaticResource NormalButton}" Grid.Column="3"/>
            <Button Content="Więcej" Style="{StaticResource NormalButton}" Grid.Column="4"/>
            <Button Click="OnUserAccountButton" Grid.Column="5" Background="Transparent" BorderThickness="0" VerticalAlignment="Top">
                <Image Source="{StaticResource user_account}" Width="70" Margin="20,30"  Height="70"/>
            </Button>
        </Grid>
    </WrapPanel>
</UserControl>
