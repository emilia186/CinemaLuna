<Window x:Class="CinemaLuna.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:CinemaLuna"
        xmlns:CustomControls="clr-namespace:CinemaLuna.UserControls"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800" MinHeight="450" MinWidth="820" Background="Black">

    <Window.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="Dictionary.xaml"/>
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </Window.Resources>

    <ScrollViewer>
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="auto"/>
                <RowDefinition Height="auto"/>
                <RowDefinition Height="auto"/>
            </Grid.RowDefinitions>
            <CustomControls:MenuControl Grid.Row="0" HorizontalAlignment="Center"/>

            <WrapPanel Orientation="Horizontal" Grid.Row="1" HorizontalAlignment="Center">
                <CustomControls:MovieControl/>
                <CustomControls:MovieControl/>
                <CustomControls:MovieControl/>
            </WrapPanel>

            <WrapPanel Orientation="Horizontal" Grid.Row="2" HorizontalAlignment="Center">
                <CustomControls:MovieControl/>
                <CustomControls:MovieControl/>
                <CustomControls:MovieControl/>
            </WrapPanel>
        </Grid>
    </ScrollViewer>
</Window>
