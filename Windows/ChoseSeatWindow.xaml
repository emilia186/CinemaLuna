<Window x:Class="CinemaLuna.Windows.ChoseSeatWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:CinemaLuna.Windows"
        xmlns:CustomControls="clr-namespace:CinemaLuna.UserControls"
        mc:Ignorable="d"
        Title="ChoseSeatWindow" Height="450" Width="990" MinHeight="500" MinWidth="900" Background="Black">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        
        <CustomControls:MenuControl Grid.Row="0" HorizontalAlignment="Center"/>
        <Label Content="{Binding Name}" Style="{StaticResource NormalText}" FontSize="30"/>
        <Label x:Name="TodayDate" Grid.Row="1" Style="{StaticResource NormalText}" FontSize="16" Padding="0" Margin="0,20" HorizontalAlignment="Left" />
    </Grid>
</Window>
